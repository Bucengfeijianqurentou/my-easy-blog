<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gb.myeasyblog.mapper.UserMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password)
        VALUES (#{username}, #{password})
    </insert>

    <select id="selectByUsername" resultType="com.gb.myeasyblog.entity.User">
        SELECT id, username, password, created_at FROM user WHERE username = #{username}
    </select>

    <select id="selectById" resultType="com.gb.myeasyblog.entity.User">
        SELECT id, username, password, created_at FROM user WHERE id = #{id}
    </select>
</mapper>